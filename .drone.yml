---
kind: pipeline
name: node-latest

steps:
- name: db-setup
  image: mysql
  commands:
  - sleep 15
  - mysql -u root -h database -p Cosc499api --execute="SELECT VERSION();"

- name: test
  image: node:latest
  commands:
  - npm install
  - npm test

services:
- name: database
  image: mysql:latest
  ports:
  - 3306
  passsword: Cosc499api
  database: helpyadb
  user: root
  environment:
     MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'

---
kind: pipeline
name: node14

steps:
- name: test
  image: node:14
  commands:
  - npm install
  - npm test

...